diff --git a/diffusion/model/builder.py b/diffusion/model/builder.py
index 49627c4..f95e39f 100644
--- a/diffusion/model/builder.py
+++ b/diffusion/model/builder.py
@@ -25,7 +25,6 @@ from transformers import AutoModelForCausalLM, AutoTokenizer, BitsAndBytesConfig
 from transformers import logging as transformers_logging
 import json
 
-from diffusion.model.dc_ae.efficientvit.ae_model_zoo import DCAE_HF
 from diffusion.model.utils import set_fp32_attention, set_grad_checkpoint
 
 MODELS = Registry("models")
@@ -103,6 +102,7 @@ def get_vae(name, model_path, device="cuda"):
             vae.config.shift_factor = 0
         return vae
     elif "dc-ae" in name:
+        from diffusion.model.dc_ae.efficientvit.ae_model_zoo import DCAE_HF
         print(colored(f"[DC-AE] Loading model from {model_path}", attrs=["bold"]))
         dc_ae = DCAE_HF.from_pretrained(model_path).to(device).eval()
         return dc_ae
diff --git a/diffusion/model/nets/sana_blocks.py b/diffusion/model/nets/sana_blocks.py
index 885063a..cb3dfd4 100644
--- a/diffusion/model/nets/sana_blocks.py
+++ b/diffusion/model/nets/sana_blocks.py
@@ -520,7 +520,8 @@ class Attention(Attention_):
             q, k = q.float(), k.float()
 
         # for rope
-        pos_thw = self.position_getter(B, t=3, h=23, w=40, device=q.device)
+        temporal, height, width = getattr(self, "mobile_video_layout", (3, 23, 40))
+        pos_thw = self.position_getter(B, t=temporal, h=height, w=width, device=q.device)
         q = self.rope(q, pos_thw)
         k = self.rope(k, pos_thw)
 
